<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Ses efektlerini uygulamak için (normal izin, kullanıcıya sorulmaz) -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    
    <!-- Ses görselleştirici için (Visualizer API, tehlikeli izin - kullanıcıya sorulur) -->
    <!-- ÖNEMLI: Sadece görselleştirme için kullanılır, kayıt yapılmaz -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    
    <!-- DJ Gesture Control için kamera izni (tehlikeli izin - kullanıcıya sorulur) -->
    <!-- ÖNEMLI: Sadece el hareketlerini algılamak için kullanılır, kayıt yapılmaz -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Boost'u arka planda (ekran kapalıyken) sürdürmek için -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <!-- Boost aktifken kalıcı durum bildirimi göstermek için (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- "Yeniden başlayınca otomatik başlat" ayarı açıksa -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Google Play gereksinimi: Reklam kimliği izni (kullanılmıyor ama beyan gerekli) -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" 
        tools:node="remove" />
    
    <!-- Parti Modu (Flash-Sync) izinleri -->
    <!-- WAKE_LOCK: Kilitli ekranda party mode çalışabilsin -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application
        android:name=".SoundBoostApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.SoundSTBoost"
        android:supportsRtl="true"
        android:localeConfig="@xml/locales_config"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:enableOnBackInvokedCallback="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.SoundSTBoost">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
            <!-- DEEP LINK for Google Assistant -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="soundboost"
                    android:host="action" />
            </intent-filter>
            
            <!-- GOOGLE ASSISTANT SHORTCUTS (v1.5.0) -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <service
            android:name=".service.BoostForegroundService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <receiver
            android:name=".service.BootReceiver"
            android:enabled="true"
            android:exported="true"
            android:directBootAware="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <!-- Quick Settings Tile (Android 7.0+) -->
        <service
            android:name=".service.VolumeBoostTileService"
            android:exported="true"
            android:icon="@drawable/ic_tile_volume_boost"
            android:label="@string/tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Premium Rezonans Widget (v1.4.0) - Glance Compose Widget -->
        <receiver
            android:name=".ui.widgets.RezonansGlanceWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_rezonans_info" />
        </receiver>

        <!-- Parti Modu Foreground Service -->
        <service
            android:name=".service.SyncForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Flash synchronization for multi-device party mode" />
        </service>

    </application>

</manifest>
